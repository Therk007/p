[phases.setup]
nixPkgs = ["python311", "python311Full", "python311Packages.pip"]

[phases.install]
cmds = [
  "python3 -m pip install --upgrade pip",
  "python3 -m pip install -r requirements.txt",
  "chmod +x RAJ"
]

[start]
cmd = "python3 a.py"
